version: '3'
services:
  app:
    build:
      dockerfile: Dockerfile.dev
      context: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/go/src/bitbucket.verifone.com/validation-service
    links:
      - mongo
    depends_on:
      - mongo
  mongo:
    image: mongo:4.0.12
    restart: always
    environment:
      - MONGO_DATA_DIR=/data/db
      - MONGO_LOG_DIR=/dev/null
    volumes:
      - mongo-data:/data/db
    command: mongod --smallfiles --logpath=/dev/null --quiet
volumes:
  mongo-data:
